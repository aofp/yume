# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build outputs
dist/
dist-*/
build/
release/
release-*/
out/
*.unpacked/

# Tauri
src-tauri/target/

# Server binaries (built per-platform, not tracked)
src-tauri/resources/server-*
src-tauri/resources/yume-server-*
# Legacy name (can remove later)
# src-tauri/resources/yurucode-server-*

# yume-cli binaries (built per-platform, not tracked)
src-tauri/resources/yume-cli-*.exe
src-tauri/resources/yume-cli-macos-*
src-tauri/resources/yume-cli-linux-*
# But DO track the .cmd wrapper for Windows
!src-tauri/resources/yume-cli-*.cmd

# yume-bin binaries (built per-platform, not tracked)
src-tauri/resources/yume-bin-*

# Logs
*.log
logs/
app.log
server.log
fresh_app.log
fixed_app.log
final_test.log

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Dynamic port files
.vite-port
.server-port
ports.tmp

# Server pid files
*.pid
server-*.pid

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Test files
test-*.html
test-*.js
test-*.mjs
temp*.txt
temp*.html
temp*.b64
testproject/

# OS files
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Package manager locks (keep package-lock.json, ignore others)
yarn.lock
pnpm-lock.yaml

# Temporary files
tmp/
temp/
*.tmp
nul
tmpclaude-*

# Server PID files
server.pid
.yume-server.pid

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
vite.config.dynamic.mjs

# Coverage and test
coverage/
*.lcov
.nyc_output
.cache
.parcel-cache

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache
# License system keypair (keep secret!)
yume_keypair.json
yume.com/

# Secret files - NEVER commit
.env.secret
yume.com/.env.secret
yume.com/licenses/
*.log
